apiVersion: v1
kind: Service
metadata:
  name: mnjhgf
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "mnjhgf"
    port: 8080
  selector:
    app: mnjhgf
